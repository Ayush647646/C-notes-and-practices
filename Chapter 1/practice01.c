#include<stdio.h>
int main()
{
    // Practice Sheet Question no. 1(a)
    float length, breadth;
    printf("Enter the required length of the rectangle \n");
    scanf("%f", &length);
    printf("Enter the required breadth of the rectangle \n");
    scanf("%f", &breadth);
    printf("Therefore, The area of the rectangle is %f", length*breadth);
    return 0;
}